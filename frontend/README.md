# Frontend

Interfaz React + Tailwind para subir archivos, corregir datos detectados y generar convenios.

## Desarrollo

```bash
npm install
npm run dev
```

## Variables opcionales

- `VITE_API_URL`: URL del backend local. Por defecto usa `http://localhost:8000`.
